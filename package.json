{
  "name": "discordthingy",
  "version": "0.5.0",
  "description": "A discord framework. But it's in typescript. That's about it.",
  "repository": "PointlessDev/DiscordThingy",
  "bugs": "https://github.com/PointlessDev/DiscordThingy/issues",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "test": "tslint -c tslint.json -p tsconfig.json --type-check 'src/**/*.ts'",
    "lint": "tslint -c tslint.json -p tsconfig.json --type-check 'src/**/*.ts'",
    "build": "tsc",
    "prepare": "tsc"
  },
  "keywords": [
    "discord",
    "discordapp"
  ],
  "engines": {
    "node": ">=8.0.0"
  },
  "author": "Pointless",
  "license": "MIT",
  "dependencies": {
    "reflect-metadata": "^0.1.10"
  },
  "peerDependencies": {
    "discord.js": "^11.1.0"
  },
  "devDependencies": {
    "@types/node": "^8.0.13",
    "tslint": "^5.5.0",
    "typescript": "^2.4.1",
    "vrsource-tslint-rules": "^5.1.0"
  }
}
